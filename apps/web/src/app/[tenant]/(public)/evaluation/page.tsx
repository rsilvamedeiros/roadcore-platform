import Link from "next/link";

export default async function EvaluationPage({ params }: { params: Promise<{ tenant: string }> }) {
  const { tenant } = await params;
  return <main className="flex-1">
    <section className="bg-[#111112] text-white"><div className="page-shell grid gap-12 py-20 lg:grid-cols-[1fr_.8fr] lg:py-28"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-primary-400">Avaliação especializada</p><h1 className="mt-5 max-w-3xl text-balance text-5xl font-semibold tracking-[-.05em] sm:text-6xl">Saiba quanto vale o seu caminhão.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-neutral-400">Analisamos configuração, conservação, histórico e mercado para chegar a uma avaliação realista e transparente.</p><Link href={`/${tenant}/sell`} className="mt-9 inline-flex rounded-xl bg-primary px-6 py-3 text-sm font-bold text-white">Solicitar avaliação →</Link></div><div className="grid grid-cols-2 gap-3 self-end">{[["Mercado","Referências atuais de compra e venda"],["Configuração","Modelo, implemento e aplicação"],["Conservação","Estado mecânico e visual"],["Documentação","Histórico e situação cadastral"]].map(([a,b])=><article key={a} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 className="font-semibold">{a}</h2><p className="mt-2 text-xs leading-5 text-neutral-500">{b}</p></article>)}</div></div></section>
    <section className="page-shell py-20"><p className="eyebrow">Como funciona</p><h2 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Uma avaliação clara, do começo ao valor final.</h2><div className="mt-10 grid gap-5 md:grid-cols-3">{[["01","Envie as informações","Conte modelo, ano, quilometragem, configuração e condição geral."],["02","Nossa equipe analisa","Comparamos o veículo com o mercado e verificamos seus diferenciais."],["03","Receba a avaliação","Apresentamos uma faixa de valor e as melhores possibilidades de negócio."]].map(([n,a,b])=><article key={n} className="panel p-7"><span className="text-sm font-black text-primary">{n}</span><h3 className="mt-8 text-lg font-semibold">{a}</h3><p className="mt-3 text-sm leading-6 text-muted">{b}</p></article>)}</div></section>
    <section className="bg-surface"><div className="page-shell py-16 text-center"><h2 className="text-3xl font-semibold tracking-tight">Pronto para descobrir o valor do seu veículo?</h2><p className="mx-auto mt-4 max-w-xl text-muted">Tenha uma referência profissional antes de vender, trocar ou planejar o próximo investimento.</p><Link href={`/${tenant}/sell`} className="mt-7 inline-flex rounded-xl bg-primary px-6 py-3 text-sm font-bold text-white">Começar agora →</Link></div></section>
  </main>;
}
