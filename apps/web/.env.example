# Public — safe to expose to the browser bundle. See docs/09-operations/environments.md.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Private — server-only, never prefix with NEXT_PUBLIC_.
# DATABASE_URL=
# SESSION_SECRET=
