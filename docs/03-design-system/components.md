# Catálogo de componentes

## Básicos

Button, Input, Select, Checkbox, Radio, Switch, Textarea, Badge, Avatar, Tooltip, Popover, Dialog, Drawer, Tabs.

## Dados

DataTable, Pagination, FilterBar, EmptyState, Skeleton, StatCard, Timeline, AuditTrail, Money, DateTime, StatusBadge.

## Domínio

VehicleCard, FreightCard, TripStatus, DriverCard, MaintenanceSummary, FinancialEntry, DocumentValidity, CommissionBreakdown.

## Regras

Componentes de domínio não devem conter regras persistentes; recebem dados e ações.
